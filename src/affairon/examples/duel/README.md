# Duel sample

This sample shows the current node DSL direction in Affairon:

- root-marked nodes
- declaration-driven child node mounting
- `@associate(...)` generated affairs
- explicit placeholder backfill via `expose_as=`
- placeholder order does not matter
- missing placeholders are allowed

Run the package entrypoint from the `duelsample` project.
